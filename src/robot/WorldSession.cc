#include "Opcodes.h"
#include "WorldSession.h"

void WorldSession::run(uint16 Opcode, WorldPacket& recvPacket)
{
//    OpcodeHandle const& opHandler = tableOpcodes[Opcode];
//    (this->*opHandler.handler)(recvPacket);
}

